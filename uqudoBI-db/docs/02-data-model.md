## Audit fields (v1)
All core tables will include:
- created_at (timestamp)
- updated_at (timestamp)
- created_by (nullable initially; user identifier from app/SSO later)
- updated_by (nullable initially; user identifier from app/SSO later)

